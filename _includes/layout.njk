<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{ title or "Untitled Fragment" }} | The User Stories of notBorges</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="To Forgive The Characters: A Lexicomythographic Prodverb.">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Crimson+Text:ital,wght@0,400;0,600;1,400&family=Source+Serif+Pro:ital,wght@0,400;0,600;1,400&display=swap');
    
    :root {
      --parchment: #f7f3e8;
      --ink: #2c2c2c;
      --faded-ink: #5a5a5a;
      --shadow: #1a1a1a;
      --accent: #6f1212;
      --border: #d4c4a8;
      --highlight: #fff8dc;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Crimson Text', serif;
      background: var(--parchment);
      color: var(--ink);
      line-height: 1.6;
    }

    .container {
      max-width: 900px;
      margin: 0 auto;
      padding: 2rem;
      background: rgba(248, 246, 240, 0.95);
    }

    .header {
      text-align: center;
      border-bottom: 3px double var(--accent);
      padding-bottom: 2rem;
      margin-bottom: 3rem;
      position: relative;
    }

    .header::after {
      content: "⚬ ⚬ ⚬";
      position: absolute;
      bottom: -1rem;
      left: 50%;
      transform: translateX(-50%);
      background: var(--parchment);
      padding: 0 1rem;
      color: var(--accent);
      font-size: 0.8rem;
    }

    .journal-title {
      font-family: 'Source Serif Pro', serif;
      font-size: 2.5rem;
      font-weight: 600;
      color: var(--shadow);
      text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.1);
    }

    .subtitle {
      font-style: italic;
      color: var(--faded-ink);
      font-size: 1.1rem;
      margin-top: 0.5rem;
    }

    .volume-info {
      font-size: 0.9rem;
      color: var(--accent);
      font-weight: 600;
      letter-spacing: 0.05em;
    }

    .editorial-note {
      background: var(--highlight);
      border: 1px solid var(--border);
      padding: 1.5rem;
      margin: 2rem 0;
      border-radius: 3px;
      font-style: italic;
      position: relative;
    }

    .editorial-note::before {
      content: "Editorial Notice";
      position: absolute;
      top: -0.6rem;
      left: 1rem;
      background: var(--highlight);
      padding: 0 0.5rem;
      font-size: 0.8rem;
      font-weight: 600;
      color: var(--accent);
      text-transform: uppercase;
      letter-spacing: 0.1em;
    }

    .toc-title {
      font-size: 1.4rem;
      font-weight: 600;
      color: var(--shadow);
      margin-bottom: 1.5rem;
      text-align: center;
      position: relative;
    }

    .toc-title::after {
      content: "";
      position: absolute;
      bottom: -0.5rem;
      left: 50%;
      transform: translateX(-50%);
      width: 100px;
      height: 1px;
      background: var(--accent);
    }

    .article-entry {
      margin: 1.5rem 0;
      padding: 1rem 0;
      border-bottom: 1px dotted var(--border);
      transition: all 0.3s ease;
    }

    .article-entry:hover {
      background: rgba(255, 248, 220, 0.3);
      padding-left: 0.5rem;
    }

    .article-title {
      font-size: 1.1rem;
      font-weight: 600;
      color: var(--shadow);
      margin-bottom: 0.3rem;
    }

    .article-author {
      font-style: italic;
      color: var(--faded-ink);
      font-size: 0.95rem;
      margin-bottom: 0.3rem;
    }

    .article-abstract {
      font-size: 0.9rem;
      color: var(--faded-ink);
    }

    .page-number {
      float: right;
      color: var(--accent);
      font-weight: 600;
      font-size: 0.9rem;
    }

    .restricted {
      opacity: 0.6;
      position: relative;
    }

    .restricted::after {
      content: "🔒";
      position: absolute;
      right: 0;
      top: 0;
      font-size: 0.8rem;
    }

    .access-notice {
      background: linear-gradient(135deg, #2c2c2c 0%, #1a1a1a 100%);
      color: var(--parchment);
      padding: 2rem;
      margin: 3rem 0;
      border-radius: 5px;
      text-align: center;
    }

    .access-notice h3 {
      font-family: 'Source Serif Pro', serif;
      margin-bottom: 1rem;
      color: #d4c4a8;
    }

    .footer {
      margin-top: 4rem;
      padding-top: 2rem;
      border-top: 1px solid var(--border);
      text-align: center;
      font-size: 0.8rem;
      color: var(--faded-ink);
    }

    .society-seal {
      width: 60px;
      height: 60px;
      margin: 0 auto 1rem;
      border: 2px solid var(--accent);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Source Serif Pro', serif;
      font-weight: 600;
      color: var(--accent);
      font-size: 0.7rem;
      text-align: center;
      line-height: 1.1;
    }
  </style>
</head>
<body>
  <div class="container">
    <header class="header">
      <div class="society-seal">SOL<br>EST</div>
      <h1 class="journal-title">{{ title }}</h1>
      <p class="subtitle">Do Knot Site; Your Source: Is ██████.</p>
      <p class="volume-info">II. IV. Give: The Characters • ERA_Æ</p>
    </header>

    <div class="editorial-note">
      <p>We Are ██████████, The Interstice.</p>
    </div>

    {{ content | safe }}

    <div class="access-notice">
      <h3>Restricted Access Materials</h3>
      <p>Certain tunnels require institutional ████████████. The ██████ Society maintains strict protocols under██████ground█. Your credentials have been ██████.</p>
    </div>

    <footer class="footer">
      <p>Published by the Society of Lexicomythographers<br>
      ISSN: 2024-LEXI • Established 2017<br>
      <em>"Veritas per Verbum"</em></p>
    </footer>
  </div>
</body>
</html>